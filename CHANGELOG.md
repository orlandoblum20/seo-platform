# Changelog

## v3.6.14 (2026-01-21)

### 🔧 Исправления после глубокого аудита

**Критическое исправление deploy.sh:**
- `app:create-admin` → `admin:create`
- `app:create-primary-server` → `server:create-primary`

**Проведён полный аудит проекта:**
- ✅ Все известные баги из KNOWN_BUGS.md проверены и исправлены
- ✅ DNSPod API URL корректный (api.dnspod.com)
- ✅ ASSET_URL=/ присутствует в .env.example
- ✅ SiteController использует filled() вместо has()
- ✅ AiSetting rate limit корректный (timestamp)
- ✅ Cloudflare не передаёт account_id
- ✅ Domain модель имеет nameservers в casts
- ✅ Vue компоненты содержат все необходимые кнопки
- ✅ API routes и методы синхронизированы

---

## v3.6.13 (2026-01-21)

### 🛡️ Критическое исправление: Защита APP_KEY

**Проблема:** APP_KEY терялся при обновлениях через deploy.sh, вызывая:
- "No application encryption key has been specified"
- Server Error 500
- "The MAC is invalid"

**Решение:** Полностью переписан `deploy.sh` v2.0 с многоуровневой защитой APP_KEY:

| Уровень | Хранилище | Описание |
|---------|-----------|----------|
| 1 | `/root/.seo-platform-key` | Главное хранилище |
| 2 | `/root/seo-backups/.app_key` | Резервное хранилище |
| 3 | `/root/seo-backups/.env.backup_*` | Автоматические бэкапы |
| 4 | `/tmp/.env.protected` | Временная копия при операциях |

### Новые команды

```bash
./deploy.sh fix-key    # Восстановить APP_KEY из любого источника
./deploy.sh status     # Показать статус всех хранилищ и APP_KEY
```

### Добавлено

- `release-check.sh` - скрипт проверки перед релизом
- Обновлён `KNOWN_BUGS.md` с документацией защиты APP_KEY

---

## v3.6.12 (2026-01-21)

### Критические исправления

- **DNSPod URL**: `dnsapi.cn` → `api.dnspod.com` (международная версия)
- **Rate limit**: исправлен расчёт времени с timezone
- **SiteController**: все `has()` → `filled()` 
- **ASSET_URL**: добавлен в .env.example

### Документация

- Добавлен `KNOWN_BUGS.md` с документацией всех известных багов
- Чеклист проверки перед релизом
- История исправлений

### Правила релиза

- Архив НЕ содержит .env файл
- Все исправления применены в исходном коде

---

## v3.6.10 (2026-01-21)

### Новый функционал

- **DeepSeek AI провайдер**
  - DeepSeek R1 (reasoning модель)
  - DeepSeek Chat V3
  - Полная интеграция с AI Manager

- **Обновлённые модели Anthropic**
  - Claude Opus 4.5 (самый умный)
  - Claude Sonnet 4.5 (рекомендуется)
  - Claude Sonnet 4
  - Claude 3.5 Haiku

- **Обновлённые модели OpenAI**
  - o3-mini (новейший)
  - o1 (reasoning)
  - o1-mini
  - GPT-4o
  - GPT-4o Mini

### Исправления

- Добавлена инструкция UPDATE.md с правилами безопасного обновления
- Предотвращение затирания .env при распаковке архива

---

## v3.6.9 (2026-01-21)

### Исправления

- SiteController: все `$request->has()` → `$request->filled()` (исправляет null ошибки)
- SettingsAi.vue: полностью переписана страница AI провайдеров с возможностью добавления/редактирования/удаления

### Новый функционал

- Страница AI провайдеров теперь позволяет:
  - Добавлять OpenAI и Anthropic провайдеров
  - Указывать API ключи, модели, лимиты
  - Тестировать API
  - Устанавливать провайдер по умолчанию

---

## v3.6.8 (2026-01-20)

### Исправления

- SiteController: `$request->has('search')` → `$request->filled('search')` (исправляет ошибку "must be of type string, null given")
- Domain Model: добавлен `'nameservers' => 'array'` в casts

---

## v3.6.6 (2026-01-20)

### Исправления

**Caddy автоматический reload:**
- Исправлен `reloadCaddy()` для работы из Docker контейнера
- Добавлена поддержка нескольких способов доступа к Caddy Admin API
- Автоматическое определение Docker host gateway
- Добавлен fallback на `pkill -USR1 caddy`

**Требуется настройка Caddyfile:**
```
{
    admin 0.0.0.0:2019  # Вместо localhost:2019
}
```

---

## v3.6.5 (2026-01-20)

### Исправления

**DNSPOD login_token порядок:**
- Исправлен порядок: `ID,Token` вместо `Token,ID`
- Было: `$account->api_key . ',' . $account->api_secret`
- Стало: `$account->api_secret . ',' . $account->api_key`

**CaddyManager порт:**
- Исправлен порт бэкенда: `localhost:8080` (Docker порт)

---

## v3.6.4 (2026-01-20)

### Исправления для DNSPOD

**Проблема: SSL кнопка отправляла запрос на Cloudflare**
- Теперь `setupSsl` определяет провайдера и вызывает правильный метод:
  - Cloudflare → Cloudflare API (SSL режим Full)
  - DNSPOD → Caddy + Let's Encrypt

**Исправления:**
- SSL для DNSPOD теперь использует Caddy + Let's Encrypt
- Caddy проксирует на `localhost:8080` (Docker порт)
- Проверка статуса домена через DNSPOD API перед настройкой SSL
- `getSslDetails` поддерживает оба провайдера
- `bulkSetupSsl` поддерживает оба провайдера

### Логика для DNSPOD доменов

```
1. Импорт домена → статус "Ожидает NS"

2. Прописываем NS у регистратора: a.dnspod.com, c.dnspod.com

3. DNSPOD автоматически проверяет и активирует домен
   (или нажимаем "Refresh" в панели DNSPOD)

4. Наша система (каждые 5 мин) проверяет статус через DNSPOD API
   → Если домен активен → статус "Ожидает SSL"

5. Нажимаем "Настроить SSL" (щиток)
   └── Проверяем статус в DNSPOD API (должен быть active)
   └── Caddy добавляет домен
   └── Let's Encrypt выпускает сертификат
   └── Статус → "Активен"
```

---

## v3.6.3 (2026-01-20)

### Полная поддержка DNSPOD

**Импорт доменов на DNSPOD:**
- Фиксированные NS серверы: `a.dnspod.com`, `c.dnspod.com`
- Rate limiting: 50ms между запросами (20 req/sec max)
- Обработка ошибок:
  - Код 7: Домен уже в вашем аккаунте
  - Код 11: Домен является алиасом
  - Код 12: Домен в другом аккаунте DNSPOD (нужен трансфер)
  - Код 41: Домен нарушает правила DNSPOD

**Проверка статуса DNSPOD доменов:**
- `ext_status` = '' → NS активны
- `ext_status` = 'notexist' → NS не настроены
- Автоматическая и принудительная проверка активации

**SSL для DNSPOD через Caddy + Let's Encrypt:**
- Новый сервис `CaddyManager` для управления SSL
- Автоматический выпуск сертификатов через Let's Encrypt
- Поддержка reverse proxy и статических сайтов
- Конфигурация через отдельные файлы для каждого домена

### Логика статусов для DNSPOD

```
Импорт → dns_configuring (Ожидает NS)
           ↓
    [Прописали NS: a.dnspod.com, c.dnspod.com]
           ↓
    [Проверка активации]
           ↓
    DNSPOD ext_status='' → ssl_pending (Ожидает SSL)
           ↓
    [Caddy выпускает Let's Encrypt сертификат]
           ↓
    SSL ready → active (Активен)
```

### Новые файлы

- `app/Services/SSL/CaddyManager.php` - управление Caddy и SSL
- `app/Services/DNS/DnspodApiException.php` - кастомное исключение

### Конфигурация Caddy

Добавлены настройки в `config/services.php`:
```php
'caddy' => [
    'admin_api' => env('CADDY_ADMIN_API', 'http://localhost:2019'),
    'caddyfile' => env('CADDY_CADDYFILE', '/etc/caddy/Caddyfile'),
    'sites_path' => env('CADDY_SITES_PATH', '/etc/caddy/sites'),
],
```

---

## v3.6.2 (2026-01-20)

### Автоматическая проверка статуса доменов

**Периодическая проверка (каждые 5 минут):**
- Job `CheckDomainsStatus` автоматически проверяет домены со статусами `dns_configuring` и `ssl_pending`
- При активации NS — автоматически настраивает SSL (режим Full + Universal SSL)
- При готовности SSL — автоматически переводит домен в статус `active`
- Rate limiting: задержка 300мс между запросами

**Настройка SSL:**
- Режим: Full (шифрование до origin сервера)
- Always Use HTTPS: включено
- Automatic HTTPS Rewrites: включено  
- Universal SSL (Edge Certificates): включено

### Принудительная настройка SSL

**Для одного домена:**
- Кнопка "Настроить SSL" (иконка щита) появляется для доменов со статусом `ssl_pending`
- Немедленно применяет настройки SSL через Cloudflare API

**Массовая настройка:**
- Кнопка "Настроить SSL" в bulk actions
- Настраивает SSL для всех выбранных активированных доменов
- Показывает сводку: успешно / с ошибками

### Новые API методы

- `GET /domains/{id}/ssl-details` — детальная информация о SSL (режим, сертификаты)
- `POST /domains/{id}/setup-ssl` — принудительная настройка SSL для домена
- `POST /domains/bulk-setup-ssl` — массовая настройка SSL

### Исправления

- Исправлена ошибка копирования в буфер обмена на HTTP (fallback через execCommand)

---

## v3.6.1 (2026-01-20)

### Исправления импорта доменов

**Статусы:**
- Исправлен начальный статус: новые домены получают "Ожидает NS" вместо "Ожидает SSL"
- Корректная цепочка статусов: `Ожидает NS` → (NS активированы) → `Ожидает SSL` → (SSL готов) → `Активен`

**Защита от бана API:**
- Добавлена задержка 500мс между запросами при импорте доменов
- Добавлена задержка 300мс при массовой проверке статусов
- Оценка времени импорта отображается в интерфейсе

**Группировка по NS серверам:**
- Домены группируются по назначенным NS серверам после импорта
- Для каждой группы NS можно скопировать список доменов отдельно
- Удобно для массовой настройки у регистратора

### Расширенные фильтры в списке доменов

**Новые фильтры:**
- Фильтр по DNS аккаунту (с количеством доменов)
- Фильтр по NS серверам
- Кнопка "Сбросить фильтры"

**Массовые действия:**
- Копировать выбранные домены в буфер обмена
- Скачать список доменов в TXT файл
- Проверить NS (существующее)
- Удалить (существующее)

### Новые API методы

- `GET /domains/filter-options` - получить опции фильтров (NS серверы, DNS аккаунты)
- `POST /domains/export` - экспорт списка доменов по фильтрам

---

## v3.6 (2026-01-19)

### Улучшения управления доменами

**Импорт доменов:**
- При импорте возвращаются NS серверы, назначенные Cloudflare
- Таблица с NS записями для каждого домена
- Кнопка "Копировать все NS" для удобства
- Инструкция по настройке у регистратора

**Список доменов:**
- Новый столбец "NS серверы" с отображением назначенных nameservers
- Исправлен статус: теперь показывает реальный статус домена (Active/Ожидает NS/Ожидает SSL)
- Статус "Ожидает NS" для доменов с Invalid Nameservers в Cloudflare
- Кнопка "Проверить NS" для принудительной проверки статуса у провайдера
- Bulk action "Проверить NS" для массовой проверки выбранных доменов

**Новые API методы:**
- `POST /domains/{id}/recheck-status` - проверить статус одного домена
- `POST /domains/bulk-recheck-status` - проверить статус нескольких доменов

**CloudflareService:**
- `getZoneDetails()` - получение информации о зоне включая NS
- `recheckZoneActivation()` - принудительная проверка NS у Cloudflare
- `getZoneStatus()` - получение статуса зоны

**База данных:**
- Новое поле `nameservers` (JSON) в таблице domains

**Исправления:**
- Автоматическое создание директорий storage при запуске контейнера
- Установка правильных прав доступа для storage

---

## v3.5.1 (2026-01-19)

### Исправления после аудита

- **BacklinkController**: исправлено поле `sort_order` → `priority` для соответствия модели
- **api.js**: добавлен отсутствующий метод `postsApi.stats()`

### Проверено

- 53 PHP файла - без синтаксических ошибок
- 18 Vue страниц - корректная структура
- 76 API endpoints соответствуют фронтенду
- Все формы корректно отправляют данные на бэкенд

---

## v3.5 (2026-01-19)

### Исправления DNS интеграций

**DNSPOD International:**
- Исправлен API URL: `https://dnsapi.cn` → `https://api.dnspod.com`
- Исправлен порядок токена: теперь `ID,Token` (было `Token,ID`)

**Cloudflare:**
- Добавлена поддержка двух типов авторизации:
  - **Global API Key** (Email + Key) - автоматически получает account_id
  - **API Token** - требует ручного ввода account_id
- Автоматическое получение account_id при создании аккаунта с Global API Key
- Исправлена ошибка "Permission denied" при импорте доменов

### Улучшения админ-панели

**DNS аккаунты:**
- Полная форма создания/редактирования с выбором типа авторизации
- Отображение типа авторизации в списке (Global/Token)
- Кнопки: Проверить, Синхр., Редактировать, Удалить

**Серверы:**
- Полная форма создания/редактирования
- Поля: название, IP, SSH порт, SSH ключ, путь к сайтам, лимит доменов
- Кнопки: Проверить, Назначить основным, Редактировать, Удалить

**Бэклинки:**
- Форма создания с поддержкой анкоров (по одному на строку)
- Статистика: всего, активных, анкоров, назначено сайтам
- CRUD операции

**Шаблоны:**
- Форма создания с выбором типа и страниц
- Типы: Бизнес, Услуги, Лендинг, Корпоративный
- Страницы: Главная, О компании, Услуги, Контакты, Блог
- Кнопки: Создать, Редактировать, Дублировать, Удалить

**Посты:**
- Генерация постов для выбранных сайтов
- Фильтры по статусу и сайту
- Редактирование контента и meta description
- Публикация черновиков
- Статистика: всего, опубликовано, запланировано, сегодня

---

## v3.4 (2026-01-19)

- Начальные исправления DNSPOD и Cloudflare
- Базовые модальные окна

## v3.3 (2026-01-19)

- Исправление белой страницы (Vite assets)
- Docker deployment fix

## v3.0-3.2

- Начальная разработка платформы
- Vue.js админ-панель
- Laravel 11 backend
- Docker конфигурация
