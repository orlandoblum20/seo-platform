# 📦 Инструкция по установке SEO Landing Platform

## Требования к серверу

| Параметр | Минимум | Рекомендуется |
|----------|---------|---------------|
| OS | Ubuntu 22.04 LTS | Ubuntu 24.04 LTS |
| CPU | 2 cores | 4 cores |
| RAM | 4 GB | 8 GB |
| Диск | 20 GB SSD | 50 GB SSD |
| Сеть | Статический IP | Статический IP |

---

## 🚀 Быстрая установка (рекомендуется)

### Шаг 1: Установка Docker (если не установлен)

```bash
# Обновляем систему
sudo apt update && sudo apt upgrade -y

# Устанавливаем Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавляем пользователя в группу docker
sudo usermod -aG docker $USER

# Перезагружаем сессию (или перелогиньтесь)
newgrp docker

# Проверяем установку
docker --version
docker compose version
```

### Шаг 2: Загрузка и установка платформы

```bash
# Скачиваем и распаковываем архив
unzip seo-platform-final-fixed.zip
cd seo-platform

# Делаем установщик исполняемым
chmod +x install.sh

# Запускаем установщик
./install.sh
```

### Шаг 3: Следуйте инструкциям установщика

Установщик запросит:
1. **IP сервера** — будет предложен автоматически
2. **Порт** — по умолчанию 8080
3. **Email администратора** — ваш email для входа
4. **Пароль администратора** — надёжный пароль

После завершения откройте в браузере:
```
http://YOUR_SERVER_IP:8080
```

---

## 🔧 Ручная установка

### 1. Подготовка

```bash
# Распаковываем архив
unzip seo-platform-final-fixed.zip
cd seo-platform

# Копируем конфигурацию
cp .env.example .env

# Редактируем настройки
nano .env
```

### 2. Настройка .env

Обязательные параметры:

```env
# URL вашего сервера
APP_URL=http://YOUR_SERVER_IP:8080

# Пароль базы данных (сгенерируйте надёжный)
DB_PASSWORD=your_secure_password_here

# IP сервера для деплоя сайтов
PRIMARY_SERVER_IP=YOUR_SERVER_IP
```

### 3. Запуск контейнеров

```bash
# Запускаем все сервисы
docker compose up -d --build

# Проверяем статус
docker compose ps
```

### 4. Инициализация приложения

```bash
# Генерируем ключ приложения
docker compose exec app php artisan key:generate

# Запускаем миграции
docker compose exec app php artisan migrate --force

# Загружаем начальные данные (шаблоны, настройки)
docker compose exec app php artisan db:seed --force

# Создаём администратора
docker compose exec app php artisan admin:create

# Кэшируем конфигурацию
docker compose exec app php artisan config:cache
docker compose exec app php artisan route:cache
docker compose exec app php artisan view:cache
```

---

## 🔐 Настройка безопасности

### Включите 2FA (рекомендуется)

1. Войдите в админ-панель
2. Перейдите в **Профиль**
3. Нажмите **Включить 2FA**
4. Отсканируйте QR-код в Google Authenticator
5. Введите код подтверждения

### Настройте Firewall

```bash
# Разрешаем только нужные порты
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 8080/tcp  # Веб-интерфейс
sudo ufw enable
```

### Настройте HTTPS (продакшен)

Для продакшена рекомендуется настроить Nginx с SSL:

```bash
# Устанавливаем Certbot
sudo apt install certbot

# Получаем сертификат
sudo certbot certonly --standalone -d your-domain.com

# Настраиваем Nginx с SSL
```

---

## ⚙️ Первоначальная настройка

После установки выполните следующие шаги в админ-панели:

### 1. Добавьте DNS аккаунт

**Настройки → DNS аккаунты → Добавить**

**Cloudflare:**
- API Email: ваш email от Cloudflare
- API Key: Global API Key из профиля Cloudflare

**DNSPOD:**
- API ID: ID из консоли DNSPOD
- API Token: токен из консоли DNSPOD

### 2. Добавьте AI провайдера

**Настройки → AI провайдеры → Добавить**

**Anthropic (Claude):**
- API Key: начинается с `sk-ant-`
- Model: `claude-3-5-sonnet-20241022`

**OpenAI:**
- API Key: начинается с `sk-`
- Model: `gpt-4-turbo-preview`

### 3. Настройте Keitaro (опционально)

**Настройки → Keitaro**

- URL: адрес вашего Keitaro
- Campaign ID: ID кампании

### 4. Настройте аналитику (опционально)

**Настройки → Аналитика**

- Яндекс.Метрика: номер счётчика
- Google Analytics: ID (G-XXXXXXXXXX)
- GTM: ID (GTM-XXXXXXX)

---

## 📊 Управление

### Полезные команды

```bash
# Просмотр логов
docker compose logs -f

# Просмотр логов конкретного сервиса
docker compose logs -f app
docker compose logs -f queue

# Перезапуск сервисов
docker compose restart

# Остановка
docker compose down

# Обновление
git pull
docker compose up -d --build
docker compose exec app php artisan migrate --force
```

### Резервное копирование

```bash
# Бэкап базы данных
docker compose exec db pg_dump -U seo_user seo_platform > backup.sql

# Восстановление
docker compose exec -T db psql -U seo_user seo_platform < backup.sql
```

---

## 🐛 Решение проблем

### Контейнеры не запускаются

```bash
# Проверяем логи
docker compose logs

# Проверяем порты
sudo netstat -tlnp | grep 8080
```

### Ошибка подключения к БД

```bash
# Перезапускаем базу
docker compose restart db

# Ждём 10 секунд
sleep 10

# Повторяем миграции
docker compose exec app php artisan migrate
```

### Очередь не работает

```bash
# Проверяем воркер
docker compose logs queue

# Перезапускаем
docker compose restart queue
```

### Права на файлы

```bash
# Исправляем права
docker compose exec app chmod -R 775 storage bootstrap/cache
docker compose exec app chown -R www-data:www-data storage bootstrap/cache
```

---

## 📞 Структура сервисов

| Сервис | Контейнер | Порт | Описание |
|--------|-----------|------|----------|
| nginx | seo-platform-nginx | 8080 | Веб-сервер |
| app | seo-platform-app | 9000 | PHP приложение |
| db | seo-platform-db | 5432 | PostgreSQL |
| redis | seo-platform-redis | 6379 | Кэш и очереди |
| queue | seo-platform-queue | — | Обработчик задач |
| scheduler | seo-platform-scheduler | — | Планировщик |

---

## 📝 Версии компонентов

- PHP: 8.3
- Laravel: 11.x
- PostgreSQL: 16
- Redis: 7
- Node.js: 20.x
- Vue.js: 3.4
- Tailwind CSS: 3.4

---

**Удачной работы с платформой!** 🚀
